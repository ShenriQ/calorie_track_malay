import { StyleSheet } from 'react-native';
import Theme from '../../../theme';

export default styles = StyleSheet.create({
    container: { backgroundColor: '#000'},
    header: { height: 80, justifyContent: 'flex-end', position: 'absolute', top: 0, left: 0 },
});